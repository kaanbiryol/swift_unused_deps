load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

# CLEAN - only uses Foundation (system module), no Bazel deps needed or declared
swift_library(
    name = "Settings",
    srcs = ["SettingsView.swift"],
    module_name = "Settings",
    deps = [],
    visibility = ["//example:__subpackages__"],
)
