load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

# UNUSED DEP - Analytics is declared but never imported
# Also tests custom module_name: Logger target has module_name="AppLogger"
swift_library(
    name = "Login",
    srcs = ["LoginViewModel.swift"],
    module_name = "Login",
    deps = [
        "//example/Core/Networking",
        "//example/Core/Analytics",      # Unused - never imported
        "//example/Core/Models",
        "//example/Core/Logger",         # Used via `import AppLogger`
    ],
    visibility = ["//example:__subpackages__"],
)
