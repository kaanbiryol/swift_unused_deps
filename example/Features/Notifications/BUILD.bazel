load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

# Declares a swift_library_group dep (Utilities) that is never used.
# The tool should report UNUSED_DEP for the group.
swift_library(
    name = "Notifications",
    srcs = ["NotificationsView.swift"],
    module_name = "Notifications",
    visibility = ["//example:__subpackages__"],
    deps = [
        "//example/Core/Models",
        "//example/Core/Utilities",
    ],
)
