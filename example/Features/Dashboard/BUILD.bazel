load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

# MISSING DIRECT DEP - Models is directly imported in source but not declared.
# Available transitively via Networking -> Models.
swift_library(
    name = "Dashboard",
    srcs = ["DashboardView.swift"],
    deps = [
        "//example/Core/Networking",
    ],
    visibility = ["//visibility:public"],
)
